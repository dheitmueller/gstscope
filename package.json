{
  "name": "gstscope",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-2.0-or-later",
  "description": "Interactive browser-based explorer for GStreamer DOT pipeline dumps",
  "type": "module",
  "engines": {
    "node": "24.x",
    "npm": ">=11"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/test-geometry.mjs",
    "check": "npm run test && npm run build && node --check dist/app.js && node scripts/validate-build.mjs",
    "dev": "npm run build && node scripts/serve.mjs"
  },
  "dependencies": {
    "@viz-js/viz": "3.30.0",
    "cytoscape": "3.34.3",
    "cytoscape-dagre": "4.0.1",
    "dagre": "0.8.5"
  }
}
